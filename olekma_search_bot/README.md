# 🤖 Бот для поиска на мояолекма.рф

Telegram-бот для поиска информации на справочно-информационном портале "Моя Олёкма" (мояолекма.рф)

## 🚀 Быстрый старт

### 1. Получение токена бота

1. Откройте Telegram и найдите @BotFather
2. Отправьте команду `/newbot`
3. Придумайте имя бота (например: "Поиск Моя Олёкма")
4. Придумайте username бота (например: `myolekma_search_bot` — обязательно заканчивается на _bot)
5. @BotFather выдаст токен вида: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`
6. **Сохраните этот токен!**

### 2. Установка

```bash
# Установите Python 3.8+ если не установлен
python --version

# Создайте папку для бота
mkdir olekma_bot
cd olekma_bot

# Создайте виртуальное окружение (рекомендуется)
python -m venv venv

# Активируйте окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 3. Настройка

1. Откройте файл `bot.py` в текстовом редакторе
2. Найдите строку:
   ```python
   BOT_TOKEN = 'YOUR_BOT_TOKEN_HERE'
   ```
3. Замените `YOUR_BOT_TOKEN_HERE` на ваш токен от @BotFather:
   ```python
   BOT_TOKEN = '123456789:ABCdefGHIjklMNOpqrsTUVwxyz'
   ```
4. Сохраните файл

### 4. Запуск

```bash
python bot.py
```

При успешном запуске вы увидите:
```
🤖 Бот запущен!
Отправьте /start в Telegram для начала работы
```

## 📱 Использование

1. Найдите вашего бота в Telegram по username (который создали)
2. Отправьте `/start`
3. Введите слово или фразу для поиска
4. Бот вернёт результаты с сайта мояолекма.рф

## 🛠 Команды бота

- `/start` — Начать работу с ботом
- `/help` — Справка по использованию

## 📝 Примеры поиска

- `библиотека` — найдёт информацию о библиотеках
- `школа` — информация о школах
- `детский сад` — детские сады
- `больница` — медицинские учреждения
- `администрация` — контакты администрации

## ⚙️ Настройка (опционально)

### Изменение сайта для поиска

Если нужно искать на другом сайте на Joomla, измените в `bot.py`:

```python
SITE_URL = 'https://ваш-сайт.ru'
```

### Ограничение количества результатов

В коде найдите:
```python
for i, result in enumerate(results[:5], 1):
```

Измените `[:5]` на нужное число (например `[:10]` для 10 результатов).

## 🐛 Ошибки и решения

### "Ошибка запроса: 403"
Сайт блокирует автоматические запросы. Попробуйте:
- Запустить бота с другого IP (VPS/сервер)
- Добавить больше заголовков в `headers` в коде

### "No module named 'telegram'"
Библиотеки не установлены:
```bash
pip install -r requirements.txt
```

### Бот не отвечает
- Проверьте правильность токена
- Убедитесь, что бот запущен (видно сообщение "Бот запущен!")
- Проверьте соединение с интернетом

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в консоли
2. Убедитесь, что сайт мояолекма.рф доступен
3. Попробуйте обновить библиотеки: `pip install --upgrade -r requirements.txt`

## 📄 Лицензия

Свободное использование.

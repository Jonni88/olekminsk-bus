# Инструкция: Загрузка проекта на GitHub

## Часть 1: Создание репозитория на GitHub

1. Открой https://github.com и войди в свой аккаунт
2. Нажми зелёную кнопку **"New"** (или "+" → "New repository")
3. Заполни поля:
   - **Repository name:** `olekminsk-bus`
   - **Description:** `Приложение расписания автобусов Олёкминска`
   - **Public** или **Private** — на выбор
   - **НЕ ставь** галочки "Add a README" и "Add .gitignore" (они уже есть в проекте)
4. Нажми **"Create repository"**

## Часть 2: Загрузка проекта (Push)

После создания репозитория GitHub покажет команды. Выполни их в терминале:

```bash
# Перейди в папку проекта
cd /home/jonni88/.openclaw/workspace/olekminsk-bus

# Добавь удалённый репозиторий (замени USERNAME на свой ник)
git remote add origin https://github.com/USERNAME/olekminsk-bus.git

# Переименуй ветку в main (опционально, но рекомендуется)
git branch -M main

# Загрузи проект на GitHub
git push -u origin main
```

**Готово!** Проект теперь на GitHub.

## Часть 3: Сборка APK в Android Studio

### Вариант A: Скачать готовый проект

```bash
# Клонируй репозиторий
git clone https://github.com/USERNAME/olekminsk-bus.git

# Открой папку в Android Studio
```

### Вариант B: Открыть существующий проект

Если проект уже на компьютере:

1. Открой **Android Studio**
2. Выбери **File → Open**
3. Найди папку `olekminsk-bus` и нажми **OK**
4. Дождись синхронизации Gradle (появится прогресс внизу)
5. Подключи телефон или запусти эмулятор
6. Нажми зелёную стрелку **Run** (или `Shift + F10`)

## Часть 4: Сборка APK для установки

Чтобы получить файл `.apk` для установки на телефон:

### Через Android Studio:

1. **Build → Build Bundle(s) / APK(s) → Build APK(s)**
2. Дождись окончания сборки
3. Внизу появится уведомление — нажми **"locate"**
4. Файл будет здесь:  
   `olekminsk-bus/app/build/outputs/apk/debug/app-debug.apk`

### Через командную строку:

```bash
cd olekminsk-bus
./gradlew assembleDebug

# APK будет здесь:
# app/build/outputs/apk/debug/app-debug.apk
```

## Часть 5: Установка на телефон

### Способ 1: Через USB

1. Включи на телефоне **Режим разработчика**:
   - Настройки → О телефоне → 7 раз нажми на "Номер сборки"
2. Включи **USB-отладку**:
   - Настройки → Система → Для разработчиков → USB-отладка
3. Подключи телефон к компьютеру
4. В Android Studio нажми **Run** — APK установится автоматически

### Способ 2: Перенос файла

1. Скопируй `app-debug.apk` на телефон (через Telegram, email, USB, облако)
2. На телефоне открой файл
3. Разреши установку из неизвестных источников
4. Установи приложение

## Часть 6: Обновление расписания

Если нужно изменить автобусы:

1. Открой файл `app/src/main/assets/schedule.json`
2. Измени время в массивах `times` (формат "HH:MM")
3. Пересобери APK (Часть 4)
4. Переустанови на телефон

**Формат JSON:**
```json
{
  "routes": [
    {
      "id": 1,
      "number": "1",
      "name": "Автовокзал — Дача",
      "interval": "20-25 мин",
      "weekday": {
        "start": "07:00",
        "end": "21:07",
        "times": ["07:00", "07:42", "07:59", "08:32", "..."]
      },
      "weekend": {
        "start": "07:00",
        "end": "20:42",
        "times": ["07:00", "07:34", "..."]
      },
      "stops": ["Автовокзал", "Центральная площадь", "..."]
    }
  ]
}
```

## Полезные команды Git

```bash
# Проверить статус
git status

# Посмотреть историю коммитов
git log --oneline

# Обновить проект с GitHub
git pull

# Откатить изменения (вернуться к последнему коммиту)
git checkout -- .

# Восстановить из бэкапа
cp app/src/main/assets/schedule.json.backup.* app/src/main/assets/schedule.json
```

## Возможные проблемы

### "Gradle sync failed"
- Проверь интернет-соединение
- В Android Studio: **File → Sync Project with Gradle Files**

### "Could not find com.android.tools..."
- Убедись, что установлен **Android SDK** через SDK Manager
- **Tools → SDK Manager → SDK Platforms** — установи Android 14 (API 34)

### "INSTALL_FAILED_UPDATE_INCOMPATIBLE"
- Удали старую версию приложения с телефона перед установкой

### Приложение не открывается
- Проверь JSON на ошибки (лишние/недостающие запятые, скобки)
- В Android Studio: **Build → Clean Project**, затем **Rebuild**

## Контакты

Если что-то не работает — напиши Агенту Джону! 🕵️

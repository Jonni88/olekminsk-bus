# Olekminsk Bus Schedule App

Приложение расписания автобусов для города Олёкминск.

## Функции

- 📍 Список всех автобусных маршрутов
- 🚌 Детальное расписание каждого маршрута
- 📅 Переключение будни/выходные
- ⏰ Показ ближайшего автобуса
- 🔴 Выделение прошедших рейсов
- 📱 Современный Material Design 3 интерфейс
- 🔌 Работает офлайн

## Системные требования

- Android 7.0+ (API 24)
- ~5 МБ свободного места

## Сборка

### Через Android Studio

1. Клонируйте репозиторий:
```bash
git clone https://github.com/ВАШ_ЮЗЕРНЕЙМ/olekminsk-bus.git
```

2. Откройте проект в Android Studio

3. Дождитесь синхронизации Gradle

4. Запустите: `Run > Run 'app'` или `Shift + F10`

### Через командную строку

```bash
# Клонирование
git clone https://github.com/ВАШ_ЮЗЕРНЕЙМ/olekminsk-bus.git
cd olekminsk-bus

# Сборка debug APK
./gradlew assembleDebug

# APK будет здесь:
# app/build/outputs/apk/debug/app-debug.apk
```

## Структура проекта

```
olekminsk-bus/
├── app/
│   ├── src/main/
│   │   ├── java/com/olekminsk/bus/
│   │   │   ├── MainActivity.kt      # Главный экран
│   │   │   ├── BusRoute.kt          # Data classes
│   │   │   └── ScheduleRepository.kt # Загрузка данных
│   │   ├── assets/
│   │   │   └── schedule.json        # Расписание автобусов
│   │   └── AndroidManifest.xml
│   └── build.gradle.kts
└── build.gradle.kts
```

## Обновление расписания

Расписание хранится в файле `app/src/main/assets/schedule.json`.

Формат:
```json
{
  "routes": [
    {
      "id": 1,
      "number": "1",
      "name": "Название маршрута",
      "interval": "15-20 мин",
      "weekday": {
        "start": "06:00",
        "end": "22:00",
        "times": ["06:00", "06:20", ...]
      },
      "weekend": {
        "start": "07:00",
        "end": "21:00",
        "times": ["07:00", "07:30", ...]
      },
      "stops": ["Остановка 1", "Остановка 2", ...]
    }
  ]
}
```

После изменения JSON пересоберите приложение.

## Лицензия

MIT License
